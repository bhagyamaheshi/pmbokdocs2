<%= form_for(@issue) do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% @issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <div class="col-lg-3"><%= f.label :issueName %></div>
    <div class="col-lg-9"><%= f.text_field :issueName, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-lg-3"></div>
    <div class="col-lg-9"><%= f.submit "Create Issue", :class => "btn btn-default" %> &nbsp;&nbsp;
    <%= link_to 'Back', issues_path, :class => "btn btn-default" %></div>
  </div>
<% end %>
